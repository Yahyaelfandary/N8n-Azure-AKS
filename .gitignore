# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc
.secrets.hcl
secrets.yaml
# Ignore Kubernetes secrets and sensitive files
secrets.yaml
*.secret.yaml
*.secrets.yaml
*.enc.yaml
*.enc.yml
*.secret.yml
*.secrets.yml
secrets/
*.env
.env.*

# Ignore Helm chart generated files
charts/
*.tgz

# Ignore Kustomize build output
kustomize-build/
*.kustomize.yaml

# Ignore kubectl output files
*.out
*.log

# Ignore local configuration files
*.local.yaml
*.local.yml
*.local.json
*.local.env

# Ignore backup files
*.bak
*.backup

# Ignore editor/IDE files
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Ignore OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini

# Ignore Docker files (if using Docker)
docker-compose.override.yml
.docker/
.dockerignore

# Ignore Terraform files (if using IaC)
*.tfstate
*.tfstate.*
.terraform/
terraform.tfvars

# Ignore Helm release files
.release/
*secrets
